"""Evaluator-only SEO benchmark assets; never import into the analysis runtime.

This package deliberately performs no corpus construction at import time.  The
runtime receives an exported, label-free JSON packet, not this package.
"""

